services:
  - type: web
    name: ai-gods-world
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m game.web_server
    envVars:
      - key: PYTHON_VERSION
        value: "3.12"
    plan: free
